export interface Mark2UBBOptions {
  uploadKey?: string;
  relativeImageBaseUrl?: string;
  math2pngPath?: string;
}

export interface MarkdownDecorateOptions {
  math2Png?: boolean;
  relativeImageBaseUrl?: string;
}